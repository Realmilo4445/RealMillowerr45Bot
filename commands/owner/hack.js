const ms = require('ms')

module.exports = {
  name: "hack",
  category: "owner",
  description: "dont use it!",
  run: async (client, message, args) => {
    if(!args[0]) return message.channel.send(`Tell me who to hack`)
    cosnt
  }
}