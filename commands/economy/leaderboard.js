const Discord = require('discord.js')
const db = require('quick.db')
const moment = require('moment')

module.exports = {
  name: "leaderboard",
  description: "global leaderboard",
  usage: "leaderboard <economy> or leaderboard <callbot>"
}